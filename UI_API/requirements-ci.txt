# Minimal, CPU-only dependency set for lint, import validation and current tests.
# Keep AI/model runtimes out of the baseline CI jobs.
fastapi==0.136.1
argon2-cffi==25.1.0
argon2-cffi-bindings==25.1.0
cryptography==49.0.0
httpx==0.28.1
mypy==2.2.0
pytest==9.1.1
python-dotenv==1.2.2
python-multipart==0.0.26
psycopg[binary]==3.3.4
# The Pilot container-security gate reads docker/compose.pilot.yaml as written.
# A gate that skips when its parser is missing reports green without checking.
PyYAML==6.0.3
requests==2.33.1
redis==8.0.1
ruff==0.15.21
types-requests==2.33.0.20260402
uvicorn==0.49.0
websockets==16.0
