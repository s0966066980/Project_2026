{
  "private": true,
  "type": "module",
  "scripts": {
    "build": "vite build",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "test:e2e": "playwright test",
    "typecheck": "tsc -p tsconfig.json",
    "syntax": "find kiosk admin shared -type f -name '*.js' -print | sort | xargs -n 1 node --check"
  },
  "dependencies": {
    "@ricky0123/vad-web": "0.0.30",
    "onnxruntime-web": "1.27.0"
  },
  "devDependencies": {
    "@playwright/test": "1.61.1",
    "@types/node": "26.1.1",
    "@vitest/coverage-v8": "4.1.10",
    "vite": "8.1.4",
    "vitest": "4.1.10",
    "typescript": "^6.0.3"
  }
}
