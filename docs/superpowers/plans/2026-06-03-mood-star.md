# 心情動態星星 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在 POS 首頁加入 1–5 顆心情星星選擇器，心情存入後端 session，注入 AI 推播、語音、猶豫彈窗的 prompt，並在 Admin 後台統計心情觸擊率與訂單明細。

**Architecture:** 選完心情後前端呼叫 `POST /api/session/mood` 寫入 session_repository；各 AI 服務透過 `mood_service.get_mood_context(session_id)` 讀取 5 個可熱改的 `MOOD_CONTEXT_1..5` settings key，拼入 prompt 前綴。mood_score=0（未選）時完全透明，不影響現有行為。

**Tech Stack:** Python/FastAPI（後端）、Vanilla JS ES modules（前端）、JSON settings（prompt 配置）

---

## 檔案對照表

| 動作 | 路徑 |
|------|------|
| 新建 | `UI_API/backend/services/mood_service.py` |
| 新建 | `UI_API/backend/routes/mood_routes.py` |
| 修改 | `UI_API/backend/repositories/session_repository.py` |
| 修改 | `UI_API/config.py` |
| 修改 | `UI_API/learning_data/settings.json` |
| 修改 | `UI_API/main.py` |
| 修改 | `UI_API/backend/services/voice_service.py` |
| 修改 | `UI_API/backend/services/ai_push_service.py` |
| 修改 | `UI_API/backend/database.py` |
| 修改 | `UI_API/backend/routes/core_routes.py` |
| 修改 | `UI_API/frontend/pos/index.html` |
| 修改 | `UI_API/frontend/pos/app.js` |
| 修改 | `UI_API/frontend/admin/admin.html` |
| 修改 | `UI_API/frontend/admin/admin.js` |

---

## Task 1: session_repository — 加入 mood 儲存

**Files:**
- Modify: `UI_API/backend/repositories/session_repository.py`

- [ ] **Step 1: 加入 mood 儲存字典與三個函式**

在 `session_repository.py` 現有內容**結尾**加入：

```python
# ── 心情評分儲存（與語音對話 session_db 分開） ──────────────────
_mood_db: dict[str, dict] = {}


def set_session_mood(session_id: str, mood_score: int, mood_label: str) -> None:
    _mood_db[session_id] = {"mood_score": mood_score, "mood_label": mood_label}


def get_session_mood(session_id: str) -> dict:
    return _mood_db.get(session_id, {"mood_score": 0, "mood_label": ""})


def clear_session_mood(session_id: str) -> None:
    _mood_db.pop(session_id, None)
```

- [ ] **Step 2: 驗證語法**

```bash
cd UI_API && python3 -m py_compile backend/repositories/session_repository.py && echo OK
```

Expected: `OK`

- [ ] **Step 3: Commit**

```bash
git add UI_API/backend/repositories/session_repository.py
git commit -m "feat(mood): add mood storage to session_repository"
```

---

## Task 2: mood_service.py — 新建服務層

**Files:**
- Create: `UI_API/backend/services/mood_service.py`

- [ ] **Step 1: 建立服務檔案**

建立 `UI_API/backend/services/mood_service.py`：

```python
"""心情服務：從 session 讀取 mood_score，回傳對應 prompt context 字串。"""
import config
from repositories import session_repository

MOOD_LABELS: dict[int, str] = {
    1: "很差",
    2: "普通",
    3: "還不錯",
    4: "很開心",
    5: "超棒",
}


def get_mood_context(session_id: str) -> str:
    """回傳要注入 AI prompt 的心情描述字串；未選（score=0）回傳空字串。"""
    mood = session_repository.get_session_mood(session_id)
    score = mood.get("mood_score", 0)
    if not score:
        return ""
    return config.get(f"MOOD_CONTEXT_{score}", "")


def get_mood_label(score: int) -> str:
    return MOOD_LABELS.get(score, "")
```

- [ ] **Step 2: 驗證語法**

```bash
cd UI_API && python3 -m py_compile backend/services/mood_service.py && echo OK
```

Expected: `OK`

- [ ] **Step 3: Commit**

```bash
git add UI_API/backend/services/mood_service.py
git commit -m "feat(mood): add mood_service with get_mood_context"
```

---

## Task 3: config.py + settings.json — 加入 MOOD_CONTEXT_1..5

**Files:**
- Modify: `UI_API/config.py`
- Modify: `UI_API/learning_data/settings.json`

- [ ] **Step 1: 在 config.py DEFAULT_SETTINGS 加入 5 個 mood context key**

在 `config.py` 的 `DEFAULT_SETTINGS` 字典裡，找到 `"BARRIER_PAYMENT_FAIL_MAX": 1,` 這一行**之後**加入：

```python
    # ── 心情星星 prompt context ──────────────────────────────────
    "MOOD_CONTEXT_1": (
        "顧客今天心情很差（1星）。優先推薦薯條、麥脆雞等撫慰系餐點；"
        "語氣溫柔體貼，例如「今天辛苦了，讓美食陪伴你」。避免強調慶祝或升級。"
    ),
    "MOOD_CONTEXT_2": (
        "顧客今天心情普通（2星）。推薦熱門主餐如大麥克、麥香魚；"
        "語氣自然親切，不過度熱情。"
    ),
    "MOOD_CONTEXT_3": (
        "顧客今天心情還不錯（3星）。推薦均衡熱門組合或套餐；"
        "語氣友善正向，可適度推薦加購。"
    ),
    "MOOD_CONTEXT_4": (
        "顧客今天心情很開心（4星）。推薦升級套餐或加大；"
        "語氣開朗，可用輕度慶祝語氣，例如「心情好，就來份大份的！」。"
    ),
    "MOOD_CONTEXT_5": (
        "顧客今天心情超棒（5星）。推薦限定款、高價位或雙份餐；"
        "語氣活潑慶祝，例如「心情超好！來份大麥克犒賞自己！」。"
    ),
```

- [ ] **Step 2: 在 settings.json 加入同樣的 5 個 key**

在 `UI_API/learning_data/settings.json` 裡，找到 `"RAG_SCORE_THRESHOLD": 0.5` 之後加入（注意 JSON 格式，前一項要有逗號）：

```json
    "MOOD_CONTEXT_1": "顧客今天心情很差（1星）。優先推薦薯條、麥脆雞等撫慰系餐點；語氣溫柔體貼，例如「今天辛苦了，讓美食陪伴你」。避免強調慶祝或升級。",
    "MOOD_CONTEXT_2": "顧客今天心情普通（2星）。推薦熱門主餐如大麥克、麥香魚；語氣自然親切，不過度熱情。",
    "MOOD_CONTEXT_3": "顧客今天心情還不錯（3星）。推薦均衡熱門組合或套餐；語氣友善正向，可適度推薦加購。",
    "MOOD_CONTEXT_4": "顧客今天心情很開心（4星）。推薦升級套餐或加大；語氣開朗，可用輕度慶祝語氣，例如「心情好，就來份大份的！」。",
    "MOOD_CONTEXT_5": "顧客今天心情超棒（5星）。推薦限定款、高價位或雙份餐；語氣活潑慶祝，例如「心情超好！來份大麥克犒賞自己！」。"
```

- [ ] **Step 3: 驗證**

```bash
cd UI_API && python3 -m py_compile config.py && echo "config OK" && \
python3 -c "import json; d=json.load(open('learning_data/settings.json')); print('settings OK, keys:', [k for k in d if k.startswith('MOOD')])"
```

Expected:
```
config OK
settings OK, keys: ['MOOD_CONTEXT_1', 'MOOD_CONTEXT_2', 'MOOD_CONTEXT_3', 'MOOD_CONTEXT_4', 'MOOD_CONTEXT_5']
```

- [ ] **Step 4: Commit**

```bash
git add UI_API/config.py UI_API/learning_data/settings.json
git commit -m "feat(mood): add MOOD_CONTEXT_1..5 to DEFAULT_SETTINGS and settings.json"
```

---

## Task 4: mood_routes.py + main.py — 新增 API endpoint

**Files:**
- Create: `UI_API/backend/routes/mood_routes.py`
- Modify: `UI_API/main.py`

- [ ] **Step 1: 建立 mood_routes.py**

建立 `UI_API/backend/routes/mood_routes.py`：

```python
"""心情評分路由。"""
from fastapi import APIRouter, Body

from repositories import session_repository
from services.mood_service import MOOD_LABELS


def create_router(deps: dict) -> APIRouter:
    router = APIRouter(tags=["mood"])

    @router.post("/api/session/mood")
    async def set_session_mood(payload: dict = Body(...)):
        """前端選完心情星星後呼叫，將 mood_score 寫入 session。"""
        session_id = str(payload.get("session_id") or "").strip()
        try:
            mood_score = int(payload.get("mood_score") or 0)
        except (ValueError, TypeError):
            mood_score = 0

        if not session_id or mood_score not in range(1, 6):
            return {"status": "error", "message": "invalid params"}

        mood_label = MOOD_LABELS.get(mood_score, "")
        session_repository.set_session_mood(session_id, mood_score, mood_label)
        return {"status": "ok", "mood_score": mood_score, "mood_label": mood_label}

    return router
```

- [ ] **Step 2: 在 main.py 匯入並註冊 mood_routes**

在 `UI_API/main.py` 找到現有的 routes import 區塊：

```python
from routes import (
    core_routes,
    emotion_routes,
    debug_routes,
    demo_routes,
    menu_routes,
    ai_push_routes,
    voice_routes,
    rag_routes,
    interaction_routes,
    realtime_routes,
)
```

改為：

```python
from routes import (
    core_routes,
    emotion_routes,
    debug_routes,
    demo_routes,
    menu_routes,
    ai_push_routes,
    voice_routes,
    rag_routes,
    interaction_routes,
    realtime_routes,
    mood_routes,
)
```

- [ ] **Step 3: 在 main.py 註冊 mood router**

在 main.py 找到其他 `app.include_router(xxx_routes.create_router(deps))` 的呼叫區塊，加入：

```python
app.include_router(mood_routes.create_router(deps))
```

（與其他 include_router 放在同一區塊即可，順序不影響功能）

- [ ] **Step 4: 驗證語法**

```bash
cd UI_API && python3 -m py_compile backend/routes/mood_routes.py && echo "route OK" && \
python3 -m py_compile main.py && echo "main OK"
```

Expected:
```
route OK
main OK
```

- [ ] **Step 5: 手動測試 endpoint（啟動服務後）**

```bash
curl -s -X POST http://127.0.0.1:8000/api/session/mood \
  -H "Content-Type: application/json" \
  -d '{"session_id":"test_sess_001","mood_score":3}' | python3 -m json.tool
```

Expected:
```json
{"status": "ok", "mood_score": 3, "mood_label": "還不錯"}
```

- [ ] **Step 6: Commit**

```bash
git add UI_API/backend/routes/mood_routes.py UI_API/main.py
git commit -m "feat(mood): add POST /api/session/mood endpoint"
```

---

## Task 5: voice_service.py — 注入心情 context

**Files:**
- Modify: `UI_API/backend/services/voice_service.py`

- [ ] **Step 1: import mood_service**

在 `voice_service.py` 頂部的 import 區塊加入：

```python
from services.mood_service import get_mood_context
```

- [ ] **Step 2: 在 handle_voice 中注入 mood context**

在 `voice_service.py` 的 `handle_voice` 函式裡，找到：

```python
    if emotion_context:
        system_prompt += "\n若有顧客情緒參考，請據此調整語氣，但不要直接提及你在分析情緒。"
```

在這段**之前**插入：

```python
    # 注入心情 context（若顧客有選心情星星）
    mood_context = get_mood_context(session_id)
    if mood_context:
        system_prompt = f"【顧客心情參考】\n{mood_context}\n\n{system_prompt}"
```

- [ ] **Step 3: 驗證語法**

```bash
cd UI_API && python3 -m py_compile backend/services/voice_service.py && echo OK
```

Expected: `OK`

- [ ] **Step 4: Commit**

```bash
git add UI_API/backend/services/voice_service.py
git commit -m "feat(mood): inject mood context into voice system prompt"
```

---

## Task 6: ai_push_service.py — 注入心情 context

**Files:**
- Modify: `UI_API/backend/services/ai_push_service.py`

- [ ] **Step 1: import mood_service**

在 `ai_push_service.py` 頂部加入：

```python
from services.mood_service import get_mood_context
```

- [ ] **Step 2: 在 generate() 中讀取並注入 mood context**

在 `generate()` 函式裡，找到建構 `user` 字串的區塊：

```python
    system = config.get("AI_PUSH_SYSTEM_PROMPT")
    user = (
        f"{rag_section}"
        "【菜單白名單】\n"
        f"{_menu_context(items)}\n\n"
        f"【本次排除 ID】{', '.join(exclude) or '無'}\n"
        "請挑 1 個適合現在推播的餐點。"
        "push_text：繁體中文、18–34 字、自然熱情促購語氣，不要出現 JSON 以外的文字。"
    )
```

改為：

```python
    system = config.get("AI_PUSH_SYSTEM_PROMPT")
    mood_context = get_mood_context(session_id)
    mood_section = f"【顧客心情參考】\n{mood_context}\n\n" if mood_context else ""
    user = (
        f"{mood_section}"
        f"{rag_section}"
        "【菜單白名單】\n"
        f"{_menu_context(items)}\n\n"
        f"【本次排除 ID】{', '.join(exclude) or '無'}\n"
        "請挑 1 個適合現在推播的餐點。"
        "push_text：繁體中文、18–34 字、自然熱情促購語氣，不要出現 JSON 以外的文字。"
    )
```

- [ ] **Step 3: 驗證語法**

```bash
cd UI_API && python3 -m py_compile backend/services/ai_push_service.py && echo OK
```

Expected: `OK`

- [ ] **Step 4: Commit**

```bash
git add UI_API/backend/services/ai_push_service.py
git commit -m "feat(mood): inject mood context into ai_push user prompt"
```

---

## Task 7: database.py — checkout log 加入 mood 欄位

**Files:**
- Modify: `UI_API/backend/database.py`

- [ ] **Step 1: import session_repository**

`database.py` 頂部已有：

```python
from repositories import log_repository, menu_repository
```

改為：

```python
from repositories import log_repository, menu_repository, session_repository
```

- [ ] **Step 2: 在 record_final_checkout 加入 mood 欄位**

在 `record_final_checkout` 函式裡，找到：

```python
    log_entry["ai_push_cart_count"] = max(0, int(ai_push_cart_count))
    log_entry["ai_push_success"] = ai_push_cart_count >= 1
    log_entry["cart_sources"] = cart_sources if isinstance(cart_sources, list) else []
    return log_repository.append_session_log(log_entry)
```

在 `return` 之前加入：

```python
    mood = session_repository.get_session_mood(session_id)
    log_entry["mood_score"] = mood.get("mood_score", 0)
    log_entry["mood_label"] = mood.get("mood_label", "")
```

- [ ] **Step 3: 驗證語法**

```bash
cd UI_API && python3 -m py_compile backend/database.py && echo OK
```

Expected: `OK`

- [ ] **Step 4: Commit**

```bash
git add UI_API/backend/database.py
git commit -m "feat(mood): write mood_score and mood_label to checkout log"
```

---

## Task 8: core_routes.py — session_stats API 加入心情統計

**Files:**
- Modify: `UI_API/backend/routes/core_routes.py`

- [ ] **Step 1: 擴充 get_session_stats 回傳心情統計**

在 `core_routes.py` 找到 `get_session_stats` 函式，找到計算 `rate` 的行：

```python
        rate = round(success_count / total, 4) if total > 0 else 0.0
```

在**之後**加入心情統計計算：

```python
        # 心情統計
        mood_sessions = sum(1 for l in logs if int(l.get("mood_score") or 0) > 0)
        mood_hit_rate = round(mood_sessions / total, 4) if total > 0 else 0.0
        mood_distribution = {str(i): 0 for i in range(1, 6)}
        for l in logs:
            score = int(l.get("mood_score") or 0)
            if 1 <= score <= 5:
                mood_distribution[str(score)] += 1
```

- [ ] **Step 2: 在 sessions list 加入 mood 欄位**

找到 sessions list comprehension：

```python
        sessions = [
            {
                "timestamp": l.get("timestamp", ""),
                "session_id": l.get("session_id", ""),
                "ai_push_cart_count": int(l.get("ai_push_cart_count", 0)),
                "ai_push_success": bool(l.get("ai_push_success", False)),
                "final_cart_ids": l.get("final_cart_ids", []),
                "cart_sources": l.get("cart_sources", []),
            }
            for l in reversed(logs)
        ]
```

改為：

```python
        sessions = [
            {
                "timestamp": l.get("timestamp", ""),
                "session_id": l.get("session_id", ""),
                "ai_push_cart_count": int(l.get("ai_push_cart_count", 0)),
                "ai_push_success": bool(l.get("ai_push_success", False)),
                "final_cart_ids": l.get("final_cart_ids", []),
                "cart_sources": l.get("cart_sources", []),
                "mood_score": int(l.get("mood_score") or 0),
                "mood_label": str(l.get("mood_label") or ""),
            }
            for l in reversed(logs)
        ]
```

- [ ] **Step 3: 在 return 加入心情統計欄位**

找到 `return {` 的回傳 dict，加入：

```python
            "mood_hit_rate": mood_hit_rate,
            "mood_sessions": mood_sessions,
            "mood_distribution": mood_distribution,
```

（與 `"success_rate": rate,` 同層）

- [ ] **Step 4: 驗證語法**

```bash
cd UI_API && python3 -m py_compile backend/routes/core_routes.py && echo OK
```

Expected: `OK`

- [ ] **Step 5: Commit**

```bash
git add UI_API/backend/routes/core_routes.py
git commit -m "feat(mood): add mood hit rate and distribution to session_stats API"
```

---

## Task 9: pos/index.html — 改版 startup overlay

**Files:**
- Modify: `UI_API/frontend/pos/index.html`

- [ ] **Step 1: 替換 startupOverlay 內容**

找到 `index.html` 裡的 `#startupOverlay` div（目前是深綠漸層 + 「智慧點餐」標題）：

```html
  <div id="startupOverlay" class="absolute inset-0 z-50 flex flex-col items-center justify-center text-white transition-opacity duration-500" style="background:linear-gradient(135deg,#13201e,#274b47)">
    <button id="startupLangBtn" class="startup-lang-btn" type="button">
      <i class="fas fa-globe"></i><span>中文</span><i class="fas fa-chevron-down"></i>
    </button>
    <div class="mb-6 w-16 h-16 rounded-full flex items-center justify-center" style="background:rgba(255,255,255,0.12)"><i class="fas fa-utensils text-3xl"></i></div>
    <h1 class="text-4xl md:text-5xl font-bold mb-3 text-center tracking-tight">智慧點餐</h1>
    <p class="text-lg mb-10" style="color:rgba(255,255,255,0.5)">AI-Powered Smart Kiosk</p>
    <div class="flex flex-col sm:flex-row gap-4 w-full px-6 md:px-0 md:w-auto items-center">
      <button id="startSystemBtn" class="btn-primary w-full md:w-auto px-12 py-4 text-xl rounded-2xl shadow-2xl relative z-50">開始點餐</button>
    </div>
  </div>
```

整段替換為：

```html
  <div id="startupOverlay" class="absolute inset-0 z-50 flex flex-col items-center justify-center transition-opacity duration-500" style="background:#FFC72C">
    <button id="startupLangBtn" class="startup-lang-btn" type="button" style="position:absolute;top:16px;right:16px;background:rgba(255,255,255,0.85);color:#1a1a1a;border-radius:20px;padding:6px 14px;font-weight:700;font-size:13px;border:none;cursor:pointer">
      <i class="fas fa-globe"></i><span style="margin:0 4px">中文</span><i class="fas fa-chevron-down"></i>
    </button>

    <!-- 標題 -->
    <div style="text-align:center;margin-bottom:24px">
      <div style="font-size:32px;margin-bottom:8px">🍟</div>
      <h1 style="font-size:28px;font-weight:900;color:#1a1a1a;margin-bottom:4px">歡迎來點今天的好心情</h1>
      <p style="font-size:13px;color:#555;margin-bottom:4px">Welcome / Start Your Order</p>
      <p style="font-size:13px;font-weight:600;color:#333">✦ 先選擇今天的心情，再開始點餐</p>
    </div>

    <!-- 心情卡 -->
    <div style="background:rgba(255,255,255,0.88);border-radius:16px;padding:24px 32px;width:min(480px,90vw);margin-bottom:20px">
      <p style="font-size:17px;font-weight:800;color:#1a1a1a;margin-bottom:4px">今天心情如何？</p>
      <p style="font-size:12px;color:#777;margin-bottom:18px">Tap the stars to share your mood</p>

      <div id="moodStarsRow" style="display:flex;justify-content:space-around;margin-bottom:12px">
        <!-- 星星由 JS renderMoodStars() 動態產生 -->
      </div>

      <div id="moodFeedbackText" style="text-align:center;font-size:13px;font-weight:700;color:#DA291C;height:18px;margin-top:4px"></div>
      <p style="font-size:11px;color:#888;text-align:center;margin-top:8px">💬 您的心情將幫助我們提供更適合的餐點</p>
    </div>

    <!-- 開始按鈕 -->
    <button id="startSystemBtn"
      style="width:min(480px,90vw);padding:18px;border-radius:50px;border:none;font-size:18px;font-weight:900;cursor:pointer;letter-spacing:1px;background:#1a1a1a;color:white;transition:background 0.25s,box-shadow 0.25s;box-shadow:0 4px 20px rgba(0,0,0,0.25)"
      type="button">開始點餐</button>
    <p id="moodSkipHint" style="font-size:12px;color:#666;margin-top:10px">或直接點「開始點餐」跳過心情選擇</p>
  </div>
```

- [ ] **Step 2: 在 `</head>` 前加入 mood 星星樣式**

找到 `index.html` 的 `</style>` 標籤（現有 `<style>` 區塊的結尾），在其**之前**加入：

```css
/* ── 心情星星 ──────────────────────────────────────── */
.mood-star-item {
  display: flex; flex-direction: column; align-items: center;
  cursor: pointer; user-select: none; -webkit-tap-highlight-color: transparent;
  transition: transform 0.15s;
}
.mood-star-item:hover { transform: scale(1.05); }
.mood-star-svg { width: 48px; height: 48px; transition: transform 0.15s, filter 0.15s; }
.mood-star-svg .star-poly { fill: #d0cdc6; transition: fill 0.15s; }
.mood-star-item.lit .star-poly { fill: #FFB300; }
.mood-star-item.lit .mood-star-svg {
  transform: scale(1.18);
  filter: drop-shadow(0 3px 8px rgba(255,179,0,0.55));
}
@keyframes mood-pop {
  0%   { transform: scale(1); }
  40%  { transform: scale(1.38); }
  70%  { transform: scale(1.06); }
  100% { transform: scale(1.18); }
}
.mood-star-item.lit.popping .mood-star-svg {
  animation: mood-pop 0.3s cubic-bezier(.34,1.56,.64,1) forwards;
}
.mood-star-label {
  font-size: 11px; font-weight: 600; color: #888; margin-top: 6px;
  transition: color 0.15s, font-weight 0.15s;
}
.mood-star-item.lit .mood-star-label { color: #DA291C; font-weight: 800; }
```

- [ ] **Step 3: 確認 index.html 可被 Node 解析（JS 不載入，只檢查 HTML 結構）**

```bash
grep -c "moodStarsRow\|startSystemBtn\|moodFeedbackText" UI_API/frontend/pos/index.html
```

Expected: `3`

- [ ] **Step 4: Commit**

```bash
git add UI_API/frontend/pos/index.html
git commit -m "feat(mood): redesign startup overlay with mood star selector"
```

---

## Task 10: pos/app.js — 心情選擇邏輯

**Files:**
- Modify: `UI_API/frontend/pos/app.js`

- [ ] **Step 1: 加入 mood 狀態變數**

在 `app.js` 頂部狀態變數區塊（`let isSystemRunning = false;` 附近），加入：

```javascript
let currentMoodScore = 0;  // 0 = 未選，1–5 = 已選
```

- [ ] **Step 2: 加入星星 label 與 emoji 常數**

緊接在 `currentMoodScore` 宣告之後加入：

```javascript
const MOOD_LABELS = ['很差', '普通', '還不錯', '很開心', '超棒'];
const MOOD_EMOJIS = ['😞', '😐', '🙂', '😊', '🤩'];
```

- [ ] **Step 3: 加入 selectMood 與 renderMoodStars 函式**

在 `app.js` 靠近底部、啟動函式區塊**之前**加入：

```javascript
// =========================================================
// 心情星星
// =========================================================
function renderMoodStars() {
  const row = document.getElementById('moodStarsRow');
  if (!row) return;

  // 初次呼叫時建立 5 顆星；之後只更新 class
  if (!row.dataset.built) {
    row.dataset.built = '1';
    for (let i = 1; i <= 5; i++) {
      const item = document.createElement('div');
      item.className = 'mood-star-item';
      item.dataset.score = String(i);
      item.innerHTML = `
        <svg class="mood-star-svg" viewBox="0 0 52 52">
          <polygon class="star-poly" points="26,4 32,18 47,18 36,28 40,43 26,34 12,43 16,28 5,18 20,18"/>
        </svg>
        <div class="mood-star-label">${MOOD_LABELS[i - 1]}</div>
      `;
      item.addEventListener('click', () => selectMood(i));
      row.appendChild(item);
    }
  }

  // 更新 lit class
  row.querySelectorAll('.mood-star-item').forEach(el => {
    const score = Number(el.dataset.score);
    const shouldBeLit = score <= currentMoodScore;
    const wasLit = el.classList.contains('lit');
    el.classList.toggle('lit', shouldBeLit);
    // 彈跳動畫：剛亮起的那顆
    if (shouldBeLit && !wasLit && score === currentMoodScore) {
      el.classList.add('popping');
      setTimeout(() => el.classList.remove('popping'), 350);
    }
  });

  // 更新按鈕與提示文字
  const btn = document.getElementById('startSystemBtn');
  const feedback = document.getElementById('moodFeedbackText');
  const skipHint = document.getElementById('moodSkipHint');
  if (currentMoodScore > 0) {
    if (btn) {
      btn.style.background = '#DA291C';
      btn.style.boxShadow = '0 4px 20px rgba(218,41,28,0.4)';
      btn.textContent = `開始點餐　${MOOD_EMOJIS[currentMoodScore - 1]} ${MOOD_LABELS[currentMoodScore - 1]}`;
    }
    if (feedback) feedback.textContent = `已選 ${currentMoodScore} 顆星`;
    if (skipHint) skipHint.style.visibility = 'hidden';
  } else {
    if (btn) {
      btn.style.background = '#1a1a1a';
      btn.style.boxShadow = '0 4px 20px rgba(0,0,0,0.25)';
      btn.textContent = '開始點餐';
    }
    if (feedback) feedback.textContent = '';
    if (skipHint) skipHint.style.visibility = 'visible';
  }
}

function selectMood(n) {
  currentMoodScore = (currentMoodScore === n) ? 0 : n;
  renderMoodStars();
}
```

- [ ] **Step 4: 在頁面初始化時呼叫 renderMoodStars**

找到 `app.js` 最底部的初始化程式（或 `DOMContentLoaded` 區塊），加入：

```javascript
renderMoodStars();
```

（若找不到明確初始化點，放在檔案最底部即可，模組載入時自動執行）

- [ ] **Step 5: 修改 ui.startBtn.onclick — 加入 mood API 呼叫**

找到：

```javascript
ui.startBtn.onclick = async () => {
  if (isAdminMode()) return;
  try {
    await loadRuntimeSettings();
```

在 `await loadRuntimeSettings();` **之前**插入：

```javascript
    // 若顧客有選心情星星，先寫入後端 session
    if (currentMoodScore > 0) {
      try {
        await fetch('/api/session/mood', {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify({ session_id: sessionId, mood_score: currentMoodScore }),
        });
      } catch (e) {
        console.warn('mood API failed (non-blocking):', e);
      }
    }
```

- [ ] **Step 6: 驗證 JS 語法**

```bash
node --check UI_API/frontend/pos/app.js && echo OK
```

Expected: `OK`

- [ ] **Step 7: Commit**

```bash
git add UI_API/frontend/pos/app.js
git commit -m "feat(mood): add mood star selection logic and API call in app.js"
```

---

## Task 11: admin.html — 加入心情統計 widget 與表格欄

**Files:**
- Modify: `UI_API/frontend/admin/admin.html`

- [ ] **Step 1: 在統計區塊加入 mood widget HTML**

在 `admin.html` 找到 `<!-- Session table -->` 這行（大約在 `<div class="adm-table-wrap">` 之前），在其**之前**插入：

```html
      <!-- 心情評分統計 -->
      <div style="background:white;border-radius:14px;padding:20px 24px;border:2px solid #DA291C;margin-bottom:20px;box-shadow:0 2px 12px rgba(218,41,28,0.08)">
        <div style="display:flex;align-items:center;justify-content:space-between;margin-bottom:16px">
          <span style="font-size:14px;font-weight:800;color:#1a2233">⭐ 心情評分統計</span>
          <span style="background:#DA291C;color:white;font-size:10px;font-weight:700;padding:3px 8px;border-radius:10px">NEW</span>
        </div>
        <div style="display:flex;gap:20px;align-items:flex-start">
          <!-- 觸擊率 -->
          <div style="background:#fff8f8;border-radius:12px;padding:14px 18px;text-align:center;min-width:130px;border:1px solid #fde0e0;flex-shrink:0">
            <div style="font-size:36px;font-weight:900;color:#DA291C;line-height:1"><span id="mood-hit-pct">—</span><span style="font-size:18px">%</span></div>
            <div style="font-size:11px;color:#888;margin-top:4px">心情觸擊率</div>
            <div id="mood-hit-detail" style="font-size:11px;color:#aaa;margin-top:6px">— / — 位顧客</div>
          </div>
          <!-- 分佈 -->
          <div style="flex:1">
            <div style="font-size:12px;font-weight:700;color:#555;margin-bottom:8px">心情分佈</div>
            <div id="mood-dist-bars"></div>
          </div>
        </div>
      </div>
```

- [ ] **Step 2: 在 session table 的 `<thead>` 加入「心情」欄標題**

找到：

```html
                <th>加入方式</th>
```

在**之後**加入：

```html
                <th>心情</th>
```

- [ ] **Step 3: Commit**

```bash
git add UI_API/frontend/admin/admin.html
git commit -m "feat(mood): add mood stats widget and mood column header to admin.html"
```

---

## Task 12: admin.js — 渲染心情統計與表格欄

**Files:**
- Modify: `UI_API/frontend/admin/admin.js`

- [ ] **Step 1: 在 loadStats 加入 mood 統計渲染**

在 `admin.js` 的 `loadStats` 函式裡，找到：

```javascript
    const sessions = data.sessions || [];
    renderTop3(sessions);
    renderTable(sessions);
```

在這段**之前**加入：

```javascript
    // 心情統計
    renderMoodStats(data);
```

- [ ] **Step 2: 加入 renderMoodStats 函式**

在 `admin.js` 找到 `function renderTop3` 的**之前**，插入：

```javascript
const MOOD_BAR_COLORS = { '1':'#ef9a9a','2':'#ffcc80','3':'#fff176','4':'#a5d6a7','5':'#81d4fa' };
const MOOD_BAR_LABELS = { '1':'★ 很差','2':'★★ 普通','3':'★★★ 還不錯','4':'★★★★ 很開心','5':'★★★★★ 超棒' };
const MOOD_CHIP_STYLES = {
  0: 'background:#f0f0f0;color:#aaa',
  1: 'background:#ffebee;color:#c62828',
  2: 'background:#fff8e1;color:#f57f17',
  3: 'background:#fffde7;color:#827717',
  4: 'background:#e8f5e9;color:#2e7d32',
  5: 'background:#e3f2fd;color:#0d47a1',
};

function renderMoodStats(data) {
  const hitPct  = document.getElementById('mood-hit-pct');
  const hitDetail = document.getElementById('mood-hit-detail');
  const barsEl  = document.getElementById('mood-dist-bars');
  if (!hitPct || !barsEl) return;

  const hitRate   = data.mood_hit_rate ?? 0;
  const moodSess  = data.mood_sessions ?? 0;
  const totalSess = data.total_sessions ?? 0;
  const dist      = data.mood_distribution ?? {};

  hitPct.textContent   = Math.round(hitRate * 100);
  hitDetail.textContent = `${moodSess} / ${totalSess} 位顧客選了心情`;

  barsEl.textContent = '';
  ['1','2','3','4','5'].forEach(k => {
    const count = dist[k] ?? 0;
    const pct   = moodSess > 0 ? Math.round((count / moodSess) * 100) : 0;
    const row   = document.createElement('div');
    row.style.cssText = 'display:flex;align-items:center;gap:8px;margin-bottom:7px';
    row.innerHTML = `
      <span style="font-size:11px;color:#444;width:80px;flex-shrink:0">${MOOD_BAR_LABELS[k]}</span>
      <div style="flex:1;height:18px;background:#f0f0f0;border-radius:9px;overflow:hidden">
        <div style="height:100%;width:${pct}%;background:${MOOD_BAR_COLORS[k]};border-radius:9px;transition:width 0.6s ease"></div>
      </div>
      <span style="font-size:11px;color:#888;width:56px;text-align:right;flex-shrink:0">${count} 次 (${pct}%)</span>
    `;
    barsEl.appendChild(row);
  });
}
```

- [ ] **Step 3: 在 renderTable 加入 mood 欄位**

在 `renderTable` 函式裡，找到 `sessions.forEach(s => {` 區塊，在最後一個 `tr.appendChild(td...)` **之後**（緊接在 `tbody.appendChild(tr)` 之前）加入：

```javascript
    // 心情欄
    const tdMood = document.createElement('td');
    const score = s.mood_score ?? 0;
    if (score > 0) {
      const chip = document.createElement('span');
      chip.style.cssText = `display:inline-block;padding:3px 8px;border-radius:12px;font-size:11px;font-weight:700;${MOOD_CHIP_STYLES[score] || ''}`;
      chip.textContent = `${'★'.repeat(score)} ${['','很差','普通','還不錯','很開心','超棒'][score]}`;
      tdMood.appendChild(chip);
    } else {
      tdMood.style.cssText = 'color:#bbb;font-size:12px';
      tdMood.textContent = '—';
    }
    tr.appendChild(tdMood);
```

- [ ] **Step 4: 驗證 JS 語法**

```bash
node --check UI_API/frontend/admin/admin.js && echo OK
```

Expected: `OK`

- [ ] **Step 5: Commit**

```bash
git add UI_API/frontend/admin/admin.js
git commit -m "feat(mood): render mood hit rate, distribution bars, and mood column in admin"
```

---

## Task 13: 整合驗收

- [ ] **Step 1: 啟動服務**

```bash
cd UI_API && conda activate emotion_ui && python main.py
```

確認啟動訊息無錯誤。

- [ ] **Step 2: POS 首頁心情選擇**

開啟 http://127.0.0.1:8000：
- 看到黃底心情星星介面
- 點第 3 顆星 → 第 1、2、3 顆亮金黃，第 4、5 顆維持灰色
- 按鈕變紅顯示「開始點餐 🙂 還不錯」
- 點同一顆（第 3 顆）→ 全部取消，按鈕回黑色
- 不選直接點「開始點餐」→ 正常進入點餐

- [ ] **Step 3: mood API 驗證**

```bash
curl -s -X POST http://127.0.0.1:8000/api/session/mood \
  -H "Content-Type: application/json" \
  -d '{"session_id":"test_mood_123","mood_score":4}' | python3 -m json.tool
```

Expected:
```json
{"status": "ok", "mood_score": 4, "mood_label": "很開心"}
```

- [ ] **Step 4: 完成一筆訂單後驗證 admin 統計**

在 POS 選 4 星心情後點餐並結帳，再開啟 http://127.0.0.1:8001：
- 心情評分統計區塊出現，觸擊率顯示 > 0%
- ★★★★ 很開心 橫條顯示 1 次
- 訂單明細表格「心情」欄顯示藍色標籤「★★★★ 很開心」

- [ ] **Step 5: 驗證未選心情不影響 AI 行為**

重新整理 POS，不選星星直接點餐，完成訂單。
Admin 統計心情欄顯示「—」；觸擊率分母增加但分子不變。

- [ ] **Step 6: 最終 commit**

```bash
git add -A
git commit -m "feat(mood): complete mood star feature integration and validation"
```
